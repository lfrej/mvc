{% extends 'base.html.twig' %}

{% block title %}Update book{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <a href="{{ path('library_show_by_id', {'id': book.getId}) }}">Show book</a>
    <h1>Update {{book.getTitle}}! 📚</h1>

    <form method="post" action="">
        <input type="text" name="title" value="{{ book.getTitle }} ">
        <input type="text" name="isbn" value="{{ book.getIsbn }}">
        <input type="text" name="author" value="{{ book.getAuthor }} ">
        <input type="submit" name="do_it" value="Update book"
        onClick="this.form.action='{{ path('library_update_post', {id: book.getId}) }}';">
    </form>
</div>
{% endblock %}
